import { Card } from 'antd';
import React from 'react';

const Header = () => {
    return (
        <div>
            <Card>
                header admin
            </Card>
        </div>
    );
};

export default Header;